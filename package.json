{
  "name": "ai-agent-business-form",
  "version": "1.0.0",
  "description": "React + Node.js web form for AI agent business consultation",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd client && npm install && cd ../server && npm install"
  },
  "keywords": ["react", "nodejs", "ai-agent", "business-form"],
  "author": "Omkar Chavan",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}